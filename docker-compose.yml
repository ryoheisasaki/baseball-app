version: "3.9"

services:
  baseball-vue:
    build: .
    container_name: baseball-vue
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true